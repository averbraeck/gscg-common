/*
 * This file is generated by jOOQ.
 */
package org.gscg.data.tables;


import java.util.Arrays;
import java.util.List;
import java.util.function.Function;

import org.gscg.data.Gscg;
import org.gscg.data.Indexes;
import org.gscg.data.Keys;
import org.gscg.data.tables.records.HandledSkuRecord;
import org.jooq.Field;
import org.jooq.ForeignKey;
import org.jooq.Function3;
import org.jooq.Identity;
import org.jooq.Index;
import org.jooq.Name;
import org.jooq.Record;
import org.jooq.Records;
import org.jooq.Row3;
import org.jooq.Schema;
import org.jooq.SelectField;
import org.jooq.Table;
import org.jooq.TableField;
import org.jooq.TableOptions;
import org.jooq.UniqueKey;
import org.jooq.impl.DSL;
import org.jooq.impl.SQLDataType;
import org.jooq.impl.TableImpl;


/**
 * This class is generated by jOOQ.
 */
@SuppressWarnings({ "all", "unchecked", "rawtypes" })
public class HandledSku extends TableImpl<HandledSkuRecord> {

    private static final long serialVersionUID = 1L;

    /**
     * The reference instance of <code>gscg.handled_sku</code>
     */
    public static final HandledSku HANDLED_SKU = new HandledSku();

    /**
     * The class holding records for this type
     */
    @Override
    public Class<HandledSkuRecord> getRecordType() {
        return HandledSkuRecord.class;
    }

    /**
     * The column <code>gscg.handled_sku.id</code>.
     */
    public final TableField<HandledSkuRecord, Integer> ID = createField(DSL.name("id"), SQLDataType.INTEGER.nullable(false).identity(true), this, "");

    /**
     * The column <code>gscg.handled_sku.sku_id</code>.
     */
    public final TableField<HandledSkuRecord, Integer> SKU_ID = createField(DSL.name("sku_id"), SQLDataType.INTEGER.nullable(false), this, "");

    /**
     * The column <code>gscg.handled_sku.transport_mode_id</code>.
     */
    public final TableField<HandledSkuRecord, Integer> TRANSPORT_MODE_ID = createField(DSL.name("transport_mode_id"), SQLDataType.INTEGER.nullable(false), this, "");

    private HandledSku(Name alias, Table<HandledSkuRecord> aliased) {
        this(alias, aliased, null);
    }

    private HandledSku(Name alias, Table<HandledSkuRecord> aliased, Field<?>[] parameters) {
        super(alias, null, aliased, parameters, DSL.comment(""), TableOptions.table());
    }

    /**
     * Create an aliased <code>gscg.handled_sku</code> table reference
     */
    public HandledSku(String alias) {
        this(DSL.name(alias), HANDLED_SKU);
    }

    /**
     * Create an aliased <code>gscg.handled_sku</code> table reference
     */
    public HandledSku(Name alias) {
        this(alias, HANDLED_SKU);
    }

    /**
     * Create a <code>gscg.handled_sku</code> table reference
     */
    public HandledSku() {
        this(DSL.name("handled_sku"), null);
    }

    public <O extends Record> HandledSku(Table<O> child, ForeignKey<O, HandledSkuRecord> key) {
        super(child, key, HANDLED_SKU);
    }

    @Override
    public Schema getSchema() {
        return aliased() ? null : Gscg.GSCG;
    }

    @Override
    public List<Index> getIndexes() {
        return Arrays.asList(Indexes.HANDLED_SKU_FK_HANDLED_SKU_SKU1_IDX, Indexes.HANDLED_SKU_FK_HANDLED_SKU_TRANSPORT_MODE1_IDX);
    }

    @Override
    public Identity<HandledSkuRecord, Integer> getIdentity() {
        return (Identity<HandledSkuRecord, Integer>) super.getIdentity();
    }

    @Override
    public UniqueKey<HandledSkuRecord> getPrimaryKey() {
        return Keys.KEY_HANDLED_SKU_PRIMARY;
    }

    @Override
    public List<UniqueKey<HandledSkuRecord>> getUniqueKeys() {
        return Arrays.asList(Keys.KEY_HANDLED_SKU_ID_UNIQUE);
    }

    @Override
    public List<ForeignKey<HandledSkuRecord, ?>> getReferences() {
        return Arrays.asList(Keys.FK_HANDLED_SKU_SKU1, Keys.FK_HANDLED_SKU_TRANSPORT_MODE1);
    }

    private transient Sku _sku;
    private transient TransportMode _transportMode;

    /**
     * Get the implicit join path to the <code>gscg.sku</code> table.
     */
    public Sku sku() {
        if (_sku == null)
            _sku = new Sku(this, Keys.FK_HANDLED_SKU_SKU1);

        return _sku;
    }

    /**
     * Get the implicit join path to the <code>gscg.transport_mode</code> table.
     */
    public TransportMode transportMode() {
        if (_transportMode == null)
            _transportMode = new TransportMode(this, Keys.FK_HANDLED_SKU_TRANSPORT_MODE1);

        return _transportMode;
    }

    @Override
    public HandledSku as(String alias) {
        return new HandledSku(DSL.name(alias), this);
    }

    @Override
    public HandledSku as(Name alias) {
        return new HandledSku(alias, this);
    }

    @Override
    public HandledSku as(Table<?> alias) {
        return new HandledSku(alias.getQualifiedName(), this);
    }

    /**
     * Rename this table
     */
    @Override
    public HandledSku rename(String name) {
        return new HandledSku(DSL.name(name), null);
    }

    /**
     * Rename this table
     */
    @Override
    public HandledSku rename(Name name) {
        return new HandledSku(name, null);
    }

    /**
     * Rename this table
     */
    @Override
    public HandledSku rename(Table<?> name) {
        return new HandledSku(name.getQualifiedName(), null);
    }

    // -------------------------------------------------------------------------
    // Row3 type methods
    // -------------------------------------------------------------------------

    @Override
    public Row3<Integer, Integer, Integer> fieldsRow() {
        return (Row3) super.fieldsRow();
    }

    /**
     * Convenience mapping calling {@link SelectField#convertFrom(Function)}.
     */
    public <U> SelectField<U> mapping(Function3<? super Integer, ? super Integer, ? super Integer, ? extends U> from) {
        return convertFrom(Records.mapping(from));
    }

    /**
     * Convenience mapping calling {@link SelectField#convertFrom(Class,
     * Function)}.
     */
    public <U> SelectField<U> mapping(Class<U> toType, Function3<? super Integer, ? super Integer, ? super Integer, ? extends U> from) {
        return convertFrom(toType, Records.mapping(from));
    }
}
