/*
 * This file is generated by jOOQ.
 */
package org.gscg.data.tables;


import java.util.Arrays;
import java.util.List;
import java.util.function.Function;

import org.gscg.data.Gscg;
import org.gscg.data.Indexes;
import org.gscg.data.Keys;
import org.gscg.data.tables.records.GameVersionGoalRecord;
import org.jooq.Field;
import org.jooq.ForeignKey;
import org.jooq.Function4;
import org.jooq.Identity;
import org.jooq.Index;
import org.jooq.Name;
import org.jooq.Record;
import org.jooq.Records;
import org.jooq.Row4;
import org.jooq.Schema;
import org.jooq.SelectField;
import org.jooq.Table;
import org.jooq.TableField;
import org.jooq.TableOptions;
import org.jooq.UniqueKey;
import org.jooq.impl.DSL;
import org.jooq.impl.SQLDataType;
import org.jooq.impl.TableImpl;


/**
 * This class is generated by jOOQ.
 */
@SuppressWarnings({ "all", "unchecked", "rawtypes" })
public class GameVersionGoal extends TableImpl<GameVersionGoalRecord> {

    private static final long serialVersionUID = 1L;

    /**
     * The reference instance of <code>gscg.game_version_goal</code>
     */
    public static final GameVersionGoal GAME_VERSION_GOAL = new GameVersionGoal();

    /**
     * The class holding records for this type
     */
    @Override
    public Class<GameVersionGoalRecord> getRecordType() {
        return GameVersionGoalRecord.class;
    }

    /**
     * The column <code>gscg.game_version_goal.id</code>.
     */
    public final TableField<GameVersionGoalRecord, Integer> ID = createField(DSL.name("id"), SQLDataType.INTEGER.nullable(false).identity(true), this, "");

    /**
     * The column <code>gscg.game_version_goal.explanation</code>.
     */
    public final TableField<GameVersionGoalRecord, String> EXPLANATION = createField(DSL.name("explanation"), SQLDataType.VARCHAR(45).nullable(false), this, "");

    /**
     * The column <code>gscg.game_version_goal.goal_id</code>.
     */
    public final TableField<GameVersionGoalRecord, Integer> GOAL_ID = createField(DSL.name("goal_id"), SQLDataType.INTEGER.nullable(false), this, "");

    /**
     * The column <code>gscg.game_version_goal.game_version_id</code>.
     */
    public final TableField<GameVersionGoalRecord, Integer> GAME_VERSION_ID = createField(DSL.name("game_version_id"), SQLDataType.INTEGER.nullable(false), this, "");

    private GameVersionGoal(Name alias, Table<GameVersionGoalRecord> aliased) {
        this(alias, aliased, null);
    }

    private GameVersionGoal(Name alias, Table<GameVersionGoalRecord> aliased, Field<?>[] parameters) {
        super(alias, null, aliased, parameters, DSL.comment(""), TableOptions.table());
    }

    /**
     * Create an aliased <code>gscg.game_version_goal</code> table reference
     */
    public GameVersionGoal(String alias) {
        this(DSL.name(alias), GAME_VERSION_GOAL);
    }

    /**
     * Create an aliased <code>gscg.game_version_goal</code> table reference
     */
    public GameVersionGoal(Name alias) {
        this(alias, GAME_VERSION_GOAL);
    }

    /**
     * Create a <code>gscg.game_version_goal</code> table reference
     */
    public GameVersionGoal() {
        this(DSL.name("game_version_goal"), null);
    }

    public <O extends Record> GameVersionGoal(Table<O> child, ForeignKey<O, GameVersionGoalRecord> key) {
        super(child, key, GAME_VERSION_GOAL);
    }

    @Override
    public Schema getSchema() {
        return aliased() ? null : Gscg.GSCG;
    }

    @Override
    public List<Index> getIndexes() {
        return Arrays.asList(Indexes.GAME_VERSION_GOAL_FK_GAME_VERSION_GOAL_GAME_VERSION1_IDX, Indexes.GAME_VERSION_GOAL_FK_SESSION_GOAL_GOAL1_IDX);
    }

    @Override
    public Identity<GameVersionGoalRecord, Integer> getIdentity() {
        return (Identity<GameVersionGoalRecord, Integer>) super.getIdentity();
    }

    @Override
    public UniqueKey<GameVersionGoalRecord> getPrimaryKey() {
        return Keys.KEY_GAME_VERSION_GOAL_PRIMARY;
    }

    @Override
    public List<UniqueKey<GameVersionGoalRecord>> getUniqueKeys() {
        return Arrays.asList(Keys.KEY_GAME_VERSION_GOAL_ID_UNIQUE);
    }

    @Override
    public List<ForeignKey<GameVersionGoalRecord, ?>> getReferences() {
        return Arrays.asList(Keys.FK_SESSION_GOAL_GOAL10, Keys.FK_GAME_VERSION_GOAL_GAME_VERSION1);
    }

    private transient Goal _goal;
    private transient GameVersion _gameVersion;

    /**
     * Get the implicit join path to the <code>gscg.goal</code> table.
     */
    public Goal goal() {
        if (_goal == null)
            _goal = new Goal(this, Keys.FK_SESSION_GOAL_GOAL10);

        return _goal;
    }

    /**
     * Get the implicit join path to the <code>gscg.game_version</code> table.
     */
    public GameVersion gameVersion() {
        if (_gameVersion == null)
            _gameVersion = new GameVersion(this, Keys.FK_GAME_VERSION_GOAL_GAME_VERSION1);

        return _gameVersion;
    }

    @Override
    public GameVersionGoal as(String alias) {
        return new GameVersionGoal(DSL.name(alias), this);
    }

    @Override
    public GameVersionGoal as(Name alias) {
        return new GameVersionGoal(alias, this);
    }

    @Override
    public GameVersionGoal as(Table<?> alias) {
        return new GameVersionGoal(alias.getQualifiedName(), this);
    }

    /**
     * Rename this table
     */
    @Override
    public GameVersionGoal rename(String name) {
        return new GameVersionGoal(DSL.name(name), null);
    }

    /**
     * Rename this table
     */
    @Override
    public GameVersionGoal rename(Name name) {
        return new GameVersionGoal(name, null);
    }

    /**
     * Rename this table
     */
    @Override
    public GameVersionGoal rename(Table<?> name) {
        return new GameVersionGoal(name.getQualifiedName(), null);
    }

    // -------------------------------------------------------------------------
    // Row4 type methods
    // -------------------------------------------------------------------------

    @Override
    public Row4<Integer, String, Integer, Integer> fieldsRow() {
        return (Row4) super.fieldsRow();
    }

    /**
     * Convenience mapping calling {@link SelectField#convertFrom(Function)}.
     */
    public <U> SelectField<U> mapping(Function4<? super Integer, ? super String, ? super Integer, ? super Integer, ? extends U> from) {
        return convertFrom(Records.mapping(from));
    }

    /**
     * Convenience mapping calling {@link SelectField#convertFrom(Class,
     * Function)}.
     */
    public <U> SelectField<U> mapping(Class<U> toType, Function4<? super Integer, ? super String, ? super Integer, ? super Integer, ? extends U> from) {
        return convertFrom(toType, Records.mapping(from));
    }
}
